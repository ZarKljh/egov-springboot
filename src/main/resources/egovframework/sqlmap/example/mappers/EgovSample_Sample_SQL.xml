<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.SampleMapper">

	<resultMap id="sample" type="egovframework.example.sample.service.SampleVO">
		<result property="articleId" column="article_id"/>
		<result property="title" column="title"/>
		<result property="content" column="content"/>
		<result property="userId" column="user_id"/>
		<result property="formId" column="form_id"/>
		<result property="status" column="status"/>
		<result property="parentArticleId" column="parent_article_id"/>
		<result property="sortOrder" column="sort_order"/>
		<result property="dept" column="dept"/>
		<result property="createdAt" column="created_at"/>
		<result property="updatedAt" column="updated_at"/>	
	</resultMap>

	<insert id="insertSample" parameterType="SampleVO">

			INSERT INTO article
				( title
				  , content
				  , user_id
				  , form_id
				  , status
				  , root_id
				  , parent_article_id
				  , sort_order
				  , dept
				  , created_at )
			VALUES ( #{title}
				  , #{content}
				  , #{userId}
				  , #{formId}
				  , #{status}
				  , #{rootId}
				  , #{parentArticleId}
				  , #{sortOrder}
				  , #{dept}	  
				  , NOW() )

	</insert>

	<update id="updateSample">  
			UPDATE article
			SET title=#{title}
				, content=#{content}
				, status=#{status}
				, updated_at=NOW()
				  WHERE article_id=#{articleId}
			
	</update>

	<delete id="deleteSample">

			DELETE FROM article
			WHERE article_id=#{articleId}

	</delete>

	<select id="selectSample" resultMap="sample">

			SELECT
				a.article_id, 
				a.title, 
				a.content, 
				a.user_id, 
				b.username,
				a.form_id, 
				a.status, 
				a.parent_article_id, 
				a.created_at, 
				a.updated_at
			FROM article a
			LEFT OUTER JOIN site_user b ON a.user_id = b.user_id
			WHERE article_id=#{articleId}
	</select>

	<select id="selectSampleList" parameterType="searchVO" resultType="egovMap">

			SELECT
				a.article_id, 
				a.title, 
				a.content, 
				a.user_id,
				b.username, 
				a.form_id, 
				a.status, 
				a.root_id,
				a.parent_article_id,
				(SELECT title FROM article WHERE article_id = a.parent_article_id) AS parent_title,
				a.sort_order,
				a.dept,
				a.created_at, 
				a.updated_at
			FROM article a
			LEFT OUTER JOIN site_user b ON a.user_id = b.user_id
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND	a.title LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	a.content LIKE '%' || #{searchKeyword} || '%'
					</when>
					<when test="searchCondition == 2">
						AND	b.username LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
			ORDER BY 
				(CASE WHEN a.root_id = 0 THEN a.article_id ELSE a.root_id END) DESC,
				a.sort_order ASC
				<!--  
		        (CASE WHEN a.parent_article_id IS NULL OR a.parent_article_id = 0 
		              THEN a.article_id 
		              ELSE a.parent_article_id 
		         END) DESC,
		        a.created_at ASC
		        -->
    		LIMIT #{recordCountPerPage} OFFSET #{firstIndex}  
	</select>

	<select id="selectSampleListTotCnt" parameterType="searchVO" resultType="int">

			SELECT COUNT(*) totcnt
			FROM article a
			LEFT OUTER JOIN site_user b ON a.user_id = b.user_id
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND a.title LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	a.content LIKE '%' || #{searchKeyword} || '%'
					</when>
					<when test="searchCondition == 2">
						AND	b.username LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
	</select>
	
	<update id="updateSortOrder" parameterType="sampleVO">
	   UPDATE article 
		    SET sort_order = sort_order + 1
		    WHERE root_id = #{rootId}         /* 같은 원문 가족들만 찾아서 */
		      AND sort_order >= #{sortOrder}  /* 내 자리를 포함해 뒤에 있는 글들을 */
		      AND article_id != #{articleId}  /* 한 칸씩 뒤로 밀기 */
	</update>
	

</mapper>