<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.AnswerMapper">
	<resultMap id="answerResult" type="egovframework.example.sample.service.AnswerVO">
	    <result property="answerId" column="answer_id"/>
	    <result property="articleId" column="article_id"/>
	    <result property="userId" column="user_id"/>
	    <result property="formId" column="form_id"/>
	    <result property="content" column="content"/>
	    <result property="adminName" column="admin_name"/> 
	    <result property="formName" column="form_name"/>
	    <result property="createdAt" column="created_at"/>
	    <result property="updatedAt" column="updated_at"/>
	</resultMap>

    <insert id="insertAnswer" parameterType="answerVO">
        INSERT INTO answer (
            article_id, 
            user_id, 
            form_id, 
            content, 
            created_at, 
            updated_at
        ) VALUES (
            #{articleId}, 
            #{userId}, 
            #{formId}, 
            #{content}, 
            NOW(), 
            NOW()
        )
    </insert>

    <update id="updateArticleStatus" parameterType="int">
        UPDATE article 
        SET status = 'COMPLETE',
            updated_at = NOW()
        WHERE article_id = #{articleId}
    </update>
    
    <delete id="deleteAnswer" parameterType="int">
	    DELETE FROM answer 
	    WHERE answer_id = #{answerId}
	</delete>

    <select id="selectAnswerList" parameterType="int" resultMap="answerResult">
        SELECT 
            a.answer_id as answerId,
            a.article_id as articleId,
            a.user_id as userId,
            a.form_id as formId,
            a.content,
            a.created_at as createdAt,
            a.updated_at as updatedAt,			
            u.user_name as adminName,
            f.form_name as formName
        FROM answer a
        LEFT JOIN site_user u ON a.user_id = u.user_id
        LEFT JOIN forms f ON a.form_id = f.form_id
        WHERE a.article_id = #{articleId}
        ORDER BY a.created_at ASC
    </select>

</mapper>